import React, { useEffect } from "react";

import { 
    pastofficerinfo
} from "../data/pastofficers";

const PastOfficers = () => {
    return (
        <div className="bg-dark-surface text-white">
        </div>
    );
};

export default PastOfficers;